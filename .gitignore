# Dependencies
node_modules/

# Build output
dist/
tsconfig.tsbuildinfo

# Handoff / design assets (grandes, no pertenecen al repo)
gestor-de-proyectos-y-tareas/
*.zip

# OS / Editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Local env
.env
.env.local

# Snapshots of data.json taken automatically before every save (safety net)
.data-backups/

# Datos reales del usuario — el repo es público; nunca se sube data.json.
# data.example.json (con datos de ejemplo) sí se versiona y sirve de plantilla
# para crear data.json automáticamente en la primera ejecución.
data.json
